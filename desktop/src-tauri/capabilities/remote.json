{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update-from-app-page",
  "description": "The page served by the local backend may check for and install updates.",
  "windows": ["main"],
  "remote": { "urls": ["http://127.0.0.1:*/*"] },
  "permissions": ["allow-check-update", "allow-install-update"]
}
